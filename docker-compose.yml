version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - MY_UID=${MY_UID}
        - MY_GID=${MY_GID}
    #restart: always
    tty: true
    env_file:
      - .env
    ports:
        - 8080:8080
    volumes:
        - .:/home/appuser/app